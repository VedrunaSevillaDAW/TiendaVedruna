import { initProductCards } from "./product-cards";

window.addEventListener("DOMContentLoaded", () => {
  // Activa la logica interactiva de todas las tarjetas de producto.
  initProductCards();
});
